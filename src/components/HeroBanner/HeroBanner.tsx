import styles from "./HeroBanner.module.scss";
const HeroBanner = () => {
  return (
    <div className={`${styles.heroBanner}`}>
      <h1>HERO SECTION</h1>
    </div>
  );
};

export default HeroBanner;
